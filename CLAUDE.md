# Project instructions

## Documentation lookups

Always use the `find-docs` skill when library/API documentation, code generation, or setup/configuration steps are needed — without waiting to be explicitly asked. This applies to any library, framework, SDK, CLI tool, or cloud service, even well-known ones, since training data may be out of date.
